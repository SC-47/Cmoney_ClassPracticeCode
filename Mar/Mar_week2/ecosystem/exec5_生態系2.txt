	第二階段 生命減少與繁殖

	// 依照生態系1增加以下內容
	
	// 建立類別Grass extends Creature 這是個可憐的東西 不能移動
	
	
	-----生命減少-----

	// 不論是否有移動 動作後該生物life--
	// 若life為0則生物死亡
	// 只要是Creature(無論馬跟草)都會減少生命

	-----繁殖-----
	
        // Creature中增加方法 breed(繁殖成功回傳一個生出來的Creature / 否則回傳null)

	// 只要某個位置沒有生物 則該位置機率(機率開發者自訂)長草

	// 只要不是草的生物在移動時都可以吃掉比自己level低的生物

	// 一旦吃到生物就->
	// 1/2的機率可以進行繁殖 生出來的小孩會生在該生物移動前的位置 且繁殖出來的生物第一回來不能移動也不會被扣生命
	// 並且 life++

	// 若移動時碰到的生物level >= 該生物則無法移動 需要選擇其他方向
	// 不然就可以移動

        // 用以下格式印出生物代表的圖示

        // g g . . . g g

        // g h h h . h g

        // . . . . . h h

        // . . . . . h g

        // . . . g . g .